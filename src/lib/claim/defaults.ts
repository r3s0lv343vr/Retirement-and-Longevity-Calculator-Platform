import { NEED_DEFAULT } from "@/lib/need/defaults";
import type { CalculatorInput } from "@/lib/engine";

/** Same lean starting point as How much. The question is only 67 vs 70. */
export const CLAIM_DEFAULT: CalculatorInput = { ...NEED_DEFAULT };
